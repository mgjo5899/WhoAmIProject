import React, { Fragment } from 'react';
import Navbar from '../layout/navbar';

const Profile = () => {
    return (
        <Fragment>
            <Navbar />
        </Fragment>
    );
}

export default Profile;